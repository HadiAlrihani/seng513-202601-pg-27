services:
  db:
    build: ./db
    container_name: postgresdb
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=${db_username}
      - POSTGRES_PASSWORD=${db_password}
      - POSTGRES_DB=${db_name}